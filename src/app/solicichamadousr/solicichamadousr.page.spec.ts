import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SolicichamadousrPage } from './solicichamadousr.page';

describe('SolicichamadousrPage', () => {
  let component: SolicichamadousrPage;
  let fixture: ComponentFixture<SolicichamadousrPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(SolicichamadousrPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
