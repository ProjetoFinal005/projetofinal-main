import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AcompchamadousrPage } from './acompchamadousr.page';

describe('AcompchamadousrPage', () => {
  let component: AcompchamadousrPage;
  let fixture: ComponentFixture<AcompchamadousrPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AcompchamadousrPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
