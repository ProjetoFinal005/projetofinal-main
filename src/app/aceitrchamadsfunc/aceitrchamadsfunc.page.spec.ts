import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AceitrchamadsfuncPage } from './aceitrchamadsfunc.page';

describe('AceitrchamadsfuncPage', () => {
  let component: AceitrchamadsfuncPage;
  let fixture: ComponentFixture<AceitrchamadsfuncPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(AceitrchamadsfuncPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
